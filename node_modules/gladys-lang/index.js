var translations = require('../../lang');

module.exports = function userLang () {
    var user = gladys.user.getById(req.session.User.id);
    sails.log.debug(user);
    return translations[user.language];
};